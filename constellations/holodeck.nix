{ config, pkgs, ... }:
{
  # поки що порожньо на рівні системи — сама панель (polybar, не waybar — той
  # план був ще з часів sway, від waybar відмовились разом із sway/i3, див.
  # CLAUDE.md) іде через home-manager, crew/bspwm.nix
}